import React, { memo } from 'react';

interface Iprops {
    
}
const RootLayout: React.FC<Iprops> = () => {
    return (
        <div>
            
        </div>
    );
};

export default memo(RootLayout);